// Installable-app layer, switched OFF. This file is loaded by the survey page (index.html) and is
// deliberately separate from it, so turning the installable app on or off never means re-sending the page.
// To turn it ON, replace this file with the version that adds the manifest/icon tags and registers /sw.js.
// To turn it OFF again, restore this file and deploy sw.js with VERSION = 'off' (see the comment in sw.js).
